export type MediaControls = {
	volume: number;
	isMuted: boolean;
	isPlaying: boolean;
	isLooping: boolean;
	isLoading: boolean;
	isWaiting: boolean;
	isAudioOnly: boolean;
	isSidePanelClosed: boolean;
};
